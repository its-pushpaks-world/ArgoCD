apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://grafana.github.io/helm-charts'
    chart: grafana
    targetRevision: 8.5.0
    helm:
      valueFiles:
        - >-
          https://raw.githubusercontent.com/its-pushpaks-world/ArgoCD/main/helm-values/grafana-values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: observability
